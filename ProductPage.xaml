<Page x:Class="Bashkircev41Size.ProductPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Bashkircev41Size"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ООО 41 размер">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="15">
            <TextBlock Text="Пользователь:" FontSize="16"/>
            <TextBox Name="FIOIB" FontSize="16" IsReadOnly="True" Margin="0,5,0,0"/>

            <TextBlock Text="Роль:" FontSize="16" Margin="0,10,0,0"/>
            <TextBox Name="RoleIB" FontSize="16" IsReadOnly="True" Margin="0,5,0,0"/>
        </StackPanel>

        <TextBlock Name="TBUserInfo"
           Grid.Row="2"
           FontSize="16"
           Margin="10"
           HorizontalAlignment="Left"
           Text=""/>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,5">
            <TextBlock Text="кол-во " FontSize="16"/>
            <TextBlock Name="TBCount" FontSize="16" Margin="3,0"/>
            <TextBlock Text=" из " FontSize="16"/>
            <TextBlock Name="TBAllRecords" FontSize="16" Margin="3,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,5">
            <StackPanel Width="200">
                <TextBlock Text="поиск по наименованию" FontSize="14"/>
                <TextBox Name="TBoxSearch" 
                     Height="25" 
                     FontSize="14" 
                     TextChanged="TBoxSearch_TextChanged"/>
            </StackPanel>
            <RadioButton Content="По возрастанию стоимости" GroupName="Sort"
                         Width="300" FontSize="15" Name="RButtonUp" Checked="RButtonUp_Checked"></RadioButton>
            <RadioButton Content="По убыванию соимости" GroupName="Sort"
                         Width="300" FontSize="15" Checked="RButtonDown_Checked"
                         Name="RButtonDown"></RadioButton>

            <StackPanel Margin="20,0,0,0" Width="200">
                <TextBlock Text="фильтрация по скидке" FontSize="14"/>
                <ComboBox Name="CBDiscount"
                      Height="25"
                      SelectionChanged="CBDiscount_SelectionChanged"
                      FontSize="14">
                    <ComboBoxItem Content="Все диапазоны"/>
                    <ComboBoxItem Content="0–9,99%"/>
                    <ComboBoxItem Content="10–14,99%"/>
                    <ComboBoxItem Content="15% и более"/>
                </ComboBox>
            </StackPanel>
        </StackPanel>

        <Button Name="OrderBtn" Content="Просмотр заказа" Visibility="Hidden" Click="OrderBtn_Click" Grid.Column="0" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Top"/>

        <ListView Grid.Row="3" Name="ProductListView">
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Добавить к заказу" Click="MenuItem_Click"></MenuItem>
                </ContextMenu>
            </ListView.ContextMenu>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="170"></ColumnDefinition>
                            <ColumnDefinition Width="800"></ColumnDefinition>
                            <ColumnDefinition Width="80"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="50"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Image Height="170" Width="170" Grid.RowSpan="4" Grid.Column="0" Stretch="Uniform" HorizontalAlignment="Center">
                            <Image.Source>
                                <Binding Path="ProductPhotoPath">
                                    <Binding.TargetNullValue>
                                        <ImageSource>
                                            /Res/picture.png
                                        </ImageSource>
                                    </Binding.TargetNullValue>
                                </Binding>
                            </Image.Source>
                        </Image>
                        <TextBlock Text="{Binding ProductName}" Grid.Column="1" Grid.Row="0" HorizontalAlignment="Left" FontSize="20" TextWrapping="Wrap" Margin="0,0,0,5"></TextBlock>
                        <TextBlock Text="{Binding ProductDescription}" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Left" FontSize="20" Margin="0,0,0,5"></TextBlock>
                        <TextBlock Text="{Binding ProductCost, StringFormat='{}{0} рублей'}" Grid.Column="1" Grid.Row="2" HorizontalAlignment="Left" FontSize="20" Margin="0,0,0,5"></TextBlock>
                        <TextBlock Text="Скидка" 
                               Grid.Column="2" Grid.Row="0" 
                               HorizontalAlignment="Center" VerticalAlignment="Bottom"
                               FontSize="20"/>
                        <TextBlock Text="{Binding ProductDiscountAmount, StringFormat={}{0}%}" 
                               Grid.Column="2" Grid.Row="1" Grid.RowSpan="2"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               FontSize="24"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>

</Page>
